# Password required to access /admin/blog and its API routes.
# Set a real, unique value in your deployment environment — this is a
# shared-secret gate, not full authentication (see lib/admin-auth.ts).
ADMIN_PASSWORD=changeme
